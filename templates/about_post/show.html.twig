{% extends 'base.html.twig' %}

{% block title %}AboutPost{% endblock %}

{% block body %}
    <h1>AboutPost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about_post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ about_post.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ about_post.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('about_post_index') }}">back to list</a>

    <a href="{{ path('about_post_edit', {'id': about_post.id}) }}">edit</a>

    {{ include('about_post/_delete_form.html.twig') }}
{% endblock %}
